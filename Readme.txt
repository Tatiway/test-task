
For view and edits:
In the project directory, you can run:  npm install
Then:  gulp watch
Runs the app in the development mode.<br>
Open (http://localhost:7000) to view it in the google chrome browser.
